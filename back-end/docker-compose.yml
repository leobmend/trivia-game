version: '3'

services:
  node:
    build: .
    volumes: 
      - ./:/app
    container_name: trivia-back
    restart: always
    ports:
      - 3001:3001
    environment:
      - PORT=3001
    command: npm run dev
